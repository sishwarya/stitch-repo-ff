kind: Rules
metadata:
  namespace: k8s-freemarker-updated by branch-07
spec:
  - name: "k8s.AddFreemarkerLabels-02-updated by branch-07"
    condition:
      deployedType: k8s.Resources
      expression: "#ctx.deployedApplication.environment.name == 'kubernetes-env'"
    processor:
      - type: freemarker
        description: "Adding first label to kubernetes spec-updated by branch-07"
        phase: BASE_DEFINITION
        merge:
          type: overlay
        parameters:
          template: |
            { "metadata-updated by branch-07" : {
               "labels-updated by branch-07": {
                 "overridenLabel-02-updated by branch-07": "initialValue-02--updated by branch-07"      
               }
              }
            }
