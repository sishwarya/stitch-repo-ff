kind: Rules
metadata:
  namespace: k8s-freemarker-updated by branch-09
spec:
  - name: "k8s.AddFreemarkerLabels-06--updated by branch-09"
    condition:
      deployedType: k8s.Resources
      expression: "#ctx.deployedApplication.environment.name == 'kubernetes-env'"
    processor:
      - type: freemarker
        description: "Adding first label to kubernetes spec-updated by branch-09"
        phase: BASE_DEFINITION
        merge:
          type: overlay
        parameters:
          template: |
            { "metadata--updated by branch-09" : {
               "labels": {
                 "overridenLabel-06": "initialValue"      
               }
              }
            }
